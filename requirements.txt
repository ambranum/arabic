anthropic>=0.40.0
pandas>=2.0.0
pyarrow>=14.0.0
certifi
pywebpush>=1.14.0    # VAPID-signed Web Push for the practice-reminder sender (pipeline/send_push.py)
pymupdf>=1.24.0      # renders the scanned reference PDFs to page images (pipeline/ref_extract.py)
